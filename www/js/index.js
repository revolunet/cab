Ext.setup({

    onReady: function() {
        alert(42);
        // var panel = new Po.master.Layout({
        //     fullscreen: true
        // });

        // load agence settings
        //         Ext.Ajax.request({
        //  url: 'json/agence.php',
        //  method: 'GET',
        //  params: {
        //      'idPublication':Po.Branding.idPublication
        //  },
        //  success: function(response, opts) {
        //          var obj = Ext.decode(response.responseText);
        //          Ext.apply(Po.Branding.settings, obj);
        //          Po.utils.removeLoadMask();
        //  }
        // });
    }

});
